# === CLI Interface ===
rich>=14.0.0
prompt_toolkit>=3.0.0

# === Core API Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.1.0

# === HTTP Client ===
httpx>=0.28.0

# === Model Gateway (LLM Abstraction) ===
litellm>=1.60.0

# === Vector Database (embedded mode — no Docker required) ===
qdrant-client>=1.13.0

# === Memory Layer ===
# Local memory extraction and storage run on Ollama + Qdrant (embedded) only.

# === PDF Parsing ===
pymupdf>=1.25.0

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.24.0

# === Agents / Memory ===
ollama>=0.6.1

# === Persistence ===
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# === Messaging ===
python-telegram-bot>=20.0

# === System Monitoring ===
psutil>=6.0.0
pywin32>=306; sys_platform == 'win32'

# === Podcast Agent (optional — move to plugin if not needed) ===
# Uncomment to enable podcast generation:
# duckduckgo-search>=7.0.0
# edge-tts>=6.1.0
# beautifulsoup4>=4.12.0
# elevenlabs>=1.0.0  # premium TTS

# === Removed dependencies ===
# arq — replaced by in-process scheduler (packages/automation/scheduler.py)
# redis — no longer needed (scheduler uses JSON file store)
# apscheduler — replaced by in-process scheduler
